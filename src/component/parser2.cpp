//
// Created by janru on 06.02.2022.
//

#include "../../lib/component/parser2.h"

using namespace calculator;

node_ptr parser2::parse(std::string str) {
    str_hdl::remove_spaces(str);

    for(auto it = str.begin(); it != str.end(); it++) {

    }

}
