#include "../../lib/component/checker.h"

using namespace std;

std::string checker::operator_check() {
    for(int i = 0; i < str.length(); i++) {
        
    }
}

checker::checker(string str) : str(str) {}