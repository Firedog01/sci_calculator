#include "../lib/sin.h"

using namespace std;

dong sin::enumerate(vector<node_ptr> args) {
    if(args.size() != 1) {
        throw logic_error("wrong number of arguments in function sin");
    } else {
        // enumerate_tree(args.at(0));
    }
}